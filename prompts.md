1. "Build a React frontend for my Node.js currency converter backend. Here is my backend code..."
2. "Use modern card elements and a clean design"
3. "Generate the full App.jsx file as a download"
4. "Add JSON API routes for favorites so the React app can use them"
5. "Convert convertController.js to return JSON instead of res.render()"
6. "Add a GET route for conversion history"

